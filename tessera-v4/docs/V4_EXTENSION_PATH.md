Standalone adapter provides preset/recipe control. Extension adapter accepts analyzer payload texture vectors.
Analyzer contract: `{ textures, confidence, meta }`.
Extension mode should hide dev controls and avoid telemetry/hard-coded secrets.
