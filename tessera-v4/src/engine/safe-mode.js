export function createSafeScene(count=1800){const s=new THREE.Scene(),c=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,100);c.position.z=9;const g=new THREE.BufferGeometry();const a=new Float32Array(count*3);for(let i=0;i<count;i++){a[i*3]=(Math.random()-.5)*12;a[i*3+1]=(Math.random()-.5)*8;a[i*3+2]=(Math.random()-.5)*6;}g.setAttribute('position',new THREE.BufferAttribute(a,3));const m=new THREE.PointsMaterial({size:.045,color:0x9ec7ff,transparent:true,opacity:.85,blending:THREE.AdditiveBlending,depthWrite:false});const p=new THREE.Points(g,m);s.add(p);return {scene:s,camera:c,points:p};}
