export function attachKeyboard(app){const m={r:'rest',t:'tracking',f:'friction',a:'arrival',h:'heart_memory',m:'mixed_weather',s:'storm_contact',d:'deep_silence',u:'unnamed_field'};addEventListener('keydown',e=>{const k=e.key.toLowerCase();if(m[k])app.setPreset(m[k]);if(k==='p')app.togglePerf();if(k===' ')app.togglePause();if(k==='c')app.capture();if(['1','2','3','4'].includes(k))app.setQuality(+k);});}
