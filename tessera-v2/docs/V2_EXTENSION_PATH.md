`extension-v2/` is a downstream integration path, not final extension product.

Done: MV3 manifest, background/content/options shells, local-only API key storage stub, explicit no telemetry/no analytics.

Stubbed: live DOM signal extraction, API calls, engine embedding into page overlays.

Plan: import shared Tessera engine modules from `tessera-v2/src` for simulator/rest mode first, then controlled live integration.
