export function seedParticles(count){const positions=new Float32Array(count*3),base=new Float32Array(count*3),hues=new Float32Array(count),sizes=new Float32Array(count),opacity=new Float32Array(count),phase=new Float32Array(count);for(let i=0;i<count;i++){const k=i*3,r=Math.pow(Math.random(),0.6)*4.9,a=Math.random()*Math.PI*2;const skew=0.82+Math.random()*0.32;base[k]=Math.cos(a)*r*skew;base[k+1]=Math.sin(a)*r*(2-skew);base[k+2]=(Math.random()-0.5)*3.4;positions[k]=base[k];positions[k+1]=base[k+1];positions[k+2]=base[k+2];hues[i]=0.53+(Math.random()-0.5)*0.22;phase[i]=Math.random()*Math.PI*2;sizes[i]=0.78+Math.random()*1.08;opacity[i]=0.28+Math.random()*0.5;}return {positions,base,hues,sizes,opacity,phase};}
