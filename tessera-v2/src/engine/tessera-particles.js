export function seedParticles(count){const positions=new Float32Array(count*3),base=new Float32Array(count*3),hues=new Float32Array(count),sizes=new Float32Array(count),opacity=new Float32Array(count),phase=new Float32Array(count);for(let i=0;i<count;i++){const k=i*3,r=Math.pow(Math.random(),0.65)*4.5,a=Math.random()*Math.PI*2;base[k]=Math.cos(a)*r;base[k+1]=Math.sin(a)*r;base[k+2]=(Math.random()-0.5)*3.1;positions[k]=base[k];positions[k+1]=base[k+1];positions[k+2]=base[k+2];hues[i]=0.54+(Math.random()-0.5)*0.18;phase[i]=Math.random()*Math.PI*2;sizes[i]=0.85+Math.random()*0.95;opacity[i]=0.3+Math.random()*0.45;}return {positions,base,hues,sizes,opacity,phase};}
