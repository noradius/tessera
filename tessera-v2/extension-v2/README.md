# extension-v2
Prepared integration target for Tessera V2.

- Uses Manifest V3 shell.
- Stores optional API key in `chrome.storage.local` only.
- No telemetry, no analytics, no hard-coded keys.
- Live DOM reading/API integration is TODO after visual engine stability.
