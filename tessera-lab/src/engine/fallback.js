export class FallbackRenderer{constructor(canvas){this.canvas=canvas;this.ctx=canvas.getContext('2d');this.count=700;this.p=[...Array(this.count)].map(()=>({x:Math.random(),y:Math.random(),z:Math.random(),vx:0,vy:0}));this.resize();addEventListener('resize',()=>this.resize());}
resize(){this.canvas.width=innerWidth*devicePixelRatio;this.canvas.height=innerHeight*devicePixelRatio;this.canvas.style.width=innerWidth+'px';this.canvas.style.height=innerHeight+'px';}
update(state,dt=0.016){if(!this.ctx)return;const c=this.ctx,w=this.canvas.width,h=this.canvas.height;c.fillStyle='rgba(2,6,12,0.22)';c.fillRect(0,0,w,h);for(const o of this.p){o.vx+=(Math.random()-.5)*0.0007*(1+state.current.tracking*2);o.vy+=(Math.random()-.5)*0.0007*(1+state.current.drift*2);o.vx*=.995;o.vy*=.995;o.x=(o.x+o.vx+1)%1;o.y=(o.y+o.vy+1)%1;const x=o.x*w,y=o.y*h;const a=0.15+state.current.aliveness*0.35+o.z*0.3;c.fillStyle=`rgba(${150+o.z*80},${175+o.z*50},${220+o.z*30},${a})`;c.fillRect(x,y,1+o.z*2,1+o.z*2);} }
}
